# Installation instructions for coding agents

Only install from a user-approved, trusted checkout. Check that macOS, AeroSpace, iTerm2, and Claude Code are available, then run `./install.sh`. Verify with `aerospace-agent-notify doctor` and `aerospace-agent-notify test-notification`. Report status and errors only: never display the contents of Claude settings, prompts, or credentials. Stop if installation fails.
