# Codex adapter

Status: Planned — not supported in v1.

A future adapter must translate Codex completion events to `aerospace-agent-notify notify --terminal TERMINAL --session-id ID --source codex`; it must not change the core queue or notification libraries.
